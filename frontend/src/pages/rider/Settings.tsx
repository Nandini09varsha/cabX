import { Bell, LogOut, Volume2 } from "lucide-react";
import { useNavigate } from "react-router-dom";
import RiderLayout from "../../layouts/RiderLayout";
import { useAuth } from "../../context/AuthContext";
import { useState } from "react";
function Toggle({on,setOn}){return <button type="button" aria-pressed={on} onClick={()=>setOn(!on)} className={`relative h-7 w-12 rounded-full ${on?"bg-primary":"bg-muted"}`}><span className={`absolute top-1 h-5 w-5 rounded-full bg-card transition-transform ${on?"translate-x-6":"translate-x-1"}`}/></button>}
export default function Settings(){const {logout}=useAuth();const nav=useNavigate();const [a,setA]=useState(true),[b,setB]=useState(true),[c,setC]=useState(false);return <RiderLayout activePage="Settings"><div className="mb-7"><h1 className="text-3xl font-black">Settings</h1><p className="mt-1 text-sm text-muted-foreground">Control your CabX rider preferences.</p></div><div className="max-w-3xl space-y-5"><section className="rounded-3xl border border-border bg-card p-6 "><h2 className="font-bold">Ride preferences</h2>{[[Bell,"Ride notifications",a,setA],[Bell,"Promotional notifications",b,setB],[Volume2,"Sound alerts",c,setC]].map(([Icon,label,on,setOn])=><div key={label} className="flex items-center gap-3 border-b border-border py-4 last:border-0 "><Icon size={18}/><span className="flex-1 text-sm font-medium">{label}</span><Toggle on={on} setOn={setOn}/></div>)}</section><section className="rounded-3xl border border-border bg-card p-6 "><h2 className="font-bold">Account</h2><button onClick={()=>{logout();nav("/")}} className="flex w-full items-center gap-3 py-4 text-left text-sm font-semibold text-red-600"><LogOut size={18}/> Logout</button></section></div></RiderLayout>}
